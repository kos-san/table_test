<%= form_with model: @user, local: true do |f| %>
  <div class="table">
    <%= f.label :名前 %><br>
    <%= f.text_field :name %>
  </div>
  </div class="table">
    <%= f.label :check %>: 
    <%= f.check_box :check %>
  </div>
  <div class="table">
    <%= f.submit '送信' %>
  </div>
<% end %>